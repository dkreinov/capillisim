# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Project runtime artifacts
# build project files (live state) — keep out of git by default
*.capproj.json
plans/
# per-rig calibration data
calibration/
# generated distance-preview images
previews/
# captured cap dataset (crops + labels + contact sheet)
dataset/
